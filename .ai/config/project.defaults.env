# Generated by .ai/tools/bootstrap.py from .ai/project.yaml.
# Commit this file; put machine-local overrides in .ai/config/project.env.

SETUP_CMD='(uv sync --locked --all-groups) && (cd '\''frontend'\'' && npm ci)'
FORMAT_CHECK_CMD='(uv run --locked ruff format --check --exclude .ai .) && (cd '\''frontend'\'' && npm run format:check)'
FORMAT_APPLY_CMD='(uv run --locked ruff format --exclude .ai .) && (cd '\''frontend'\'' && npm run format)'
LINT_CMD='(uv run --locked ruff check --exclude .ai .) && (uv run --locked mypy --exclude '\''^\.ai/'\'' .) && (cd '\''frontend'\'' && npm run lint) && (cd '\''frontend'\'' && npm run typecheck) && (find . -type f -name '\''*.sh'\'' -not -path '\''./.git/*'\'' -not -path '\''./.ai/*'\'' -print0 | xargs -0 -r shellcheck)'
TEST_CMD='(uv run --locked pytest) && (cd '\''frontend'\'' && npm run test) && (test -d tests/shell && bats tests/shell)'
SECURITY_CMD='(uv run --locked bandit -q -r . -x ./.ai,./tests,./.venv,./venv)'
DEPENDENCY_SCAN_CMD='(uv run --locked pip-audit) && (cd '\''frontend'\'' && npm audit --audit-level=high)'
BUILD_CMD='(uv build) && (cd '\''frontend'\'' && npm run build)'
REQUIRE_SETUP=1
REQUIRE_FORMAT_CHECK=1
REQUIRE_LINT=1
REQUIRE_TEST=1
REQUIRE_SECURITY=1
REQUIRE_DEPENDENCY_POLICY=1
REQUIRE_DEPENDENCY_SCANNERS=1
REQUIRE_BUILD=1
