# Generated by .ai/tools/bootstrap.py from .ai/project.yaml.
# Commit this file; put machine-local overrides in .ai/config/project.env.

SETUP_CMD='(UV_PYTHON='\''3.13'\'' uv sync --locked --all-groups) && (cd '\''frontend'\'' && npm ci)'
FORMAT_CHECK_CMD='(UV_PYTHON='\''3.13'\'' uv run --locked ruff format --check '\''backend'\'' tests) && (cd '\''frontend'\'' && npm run format:check)'
FORMAT_APPLY_CMD='(UV_PYTHON='\''3.13'\'' uv run --locked ruff format '\''backend'\'' tests) && (cd '\''frontend'\'' && npm run format)'
LINT_CMD='(UV_PYTHON='\''3.13'\'' uv run --locked ruff check '\''backend'\'' tests) && (UV_PYTHON='\''3.13'\'' uv run --locked mypy '\''backend'\'' tests) && (cd '\''frontend'\'' && npm run lint) && (cd '\''frontend'\'' && npm run typecheck) && (find . -type f -name '\''*.sh'\'' -not -path '\''./.git/*'\'' -not -path '\''./.ai/*'\'' -print0 | xargs -0 -r shellcheck)'
TEST_CMD='(UV_PYTHON='\''3.13'\'' uv run --locked python -m pytest) && (cd '\''frontend'\'' && npm run test) && (test -d tests/shell || { printf '\''Bash scripts were found, but tests/shell is missing. Add Bats tests or disable stacks.bash.enabled.\n'\'' >&2; exit 1; }; bats tests/shell)'
SECURITY_CMD='(UV_PYTHON='\''3.13'\'' uv run --locked bandit -q -r '\''backend'\'') && (gitleaks detect --source . --no-banner)'
DEPENDENCY_SCAN_CMD='(UV_PYTHON='\''3.13'\'' uv run --locked pip-audit) && (cd '\''frontend'\'' && npm audit --audit-level=high)'
BUILD_CMD='(UV_PYTHON='\''3.13'\'' uv build) && (cd '\''frontend'\'' && npm run build)'
BROWSER_REVIEW_CMD='node .ai/tools/ui-browser-review.mjs browser'
VISUAL_REGRESSION_CMD='node .ai/tools/ui-browser-review.mjs visual-regression'
VISUAL_REGRESSION_ENABLED='1'
ACCESSIBILITY_CMD='node .ai/tools/ui-browser-review.mjs accessibility'
ACCESSIBILITY_ENABLED='1'
REQUIRE_SETUP=1
REQUIRE_FORMAT_CHECK=1
REQUIRE_LINT=1
REQUIRE_TEST=1
REQUIRE_SECURITY=1
REQUIRE_DEPENDENCY_POLICY=1
REQUIRE_DEPENDENCY_SCANNERS=1
REQUIRE_BUILD=1
