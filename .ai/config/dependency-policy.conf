# Committed dependency-policy defaults. Local configuration may enable stricter
# allow-list mode but cannot weaken required lockfile or source-version checks.
DEPENDENCY_ALLOWLIST_MODE=0
REQUIRE_LOCKFILES=1
REJECT_FLOATING_VERSIONS=1
